---
# Handlers for bind role

- name: Restart named
  ansible.builtin.systemd:
    name: named
    state: restarted
    daemon_reload: true

- name: Reload named
  ansible.builtin.systemd:
    name: named
    state: reloaded
